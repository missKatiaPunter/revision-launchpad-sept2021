//Try using different ways of making functions

const subtract = (a,b) => {};

// write a function that divides whole numbers

const divide = (numerator, denominator) => {
}

// write a function that divides whole numbers

const isDivisibleBy = (numerator, denominator) => {
}

// write a function that takes a numeric score and grades
// if less than 40 - a Fail
// if more or equal to 40 but less than 50 - a Pass
// if more or equal to 50 but less than 70 - a Merit
// if more than 70 - a Distinction

function gradeAssignment(score){

}

// write a function that trims the white space at the start and end of a string

const trimString = (str) => {};

module.exports = { 
    subtract,
    divide,
    isDivisibleBy,
    gradeAssignment,
    trimString
};
